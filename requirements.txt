# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1

# Twilio
twilio==9.3.2

# LangGraph / LangChain
langgraph==0.2.28
langchain==0.3.1
langchain-openai==0.2.1

# LLM
openai==1.57.0  # 1.52+ : httpx 0.28 proxies 호환 수정

# STT
deepgram-sdk==3.7.0

# VAD — Silero VAD (v6.2+, 2026-04-30 채택, ML 기반 — WebRTC 대체).
# 짧은 발화 + 긴 trailing silence 청크에서 WebRTC bulk ratio gate 가 reject 하던 문제 해결.
# torch 는 ML 스택에서 이미 설치 (BGE-M3 / TitaNet). onnxruntime 도 ArcFace 용으로 존재.
silero-vad>=6.2

# WebRTC VAD (rollback 안전판) — call.py / vad_node.py 의 import 1줄 환원으로 즉시 복구.
# Windows 사전 컴파일 바이너리 — Linux 배포 시 webrtcvad==2.0.10 으로 교체.
webrtcvad-wheels==2.0.14

# TTS — Azure Speech SDK (μ-law 8kHz 네이티브 출력, Twilio 호환)
azure-cognitiveservices-speech==1.40.0

# Speaker Verification — TitaNet-L ONNX (onnxruntime 은 ArcFace 와 공유, 아래 ArcFace 섹션 참조)

# Embedding / Vector DB
chromadb==0.5.23

# Database
asyncpg==0.29.0

# Cache
redis[asyncio]==5.1.0

# Config
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
email-validator==2.2.0

# Admin Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Audio
numpy==2.4.4

# PyTorch + CUDA — cu124 인덱스에서 설치 필요
# pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# OCR
pytesseract==0.3.13
Pillow==10.4.0

# Face Auth (M3+) — ArcFace via insightface + ONNX Runtime (CPU)
insightface==0.7.3
onnxruntime==1.19.2
opencv-python-headless==4.10.0.84

# Async Worker (TBD: Celery or Redis Stream)
celery==5.4.0

# SMS
solapi==5.0.3

# tokenizers: chromadb<=0.20.3 요구 vs transformers>=0.22.0 요구 충돌
# → transformers(BGE-M3) 우선, chromadb는 런타임 정상 동작 확인
tokenizers==0.22.2

# HTTP Client
httpx==0.27.2

# Utils
python-multipart==0.0.12

# PDF Processing
# opendataloader-pdf: Java 기반 파서 — JSON tree (heading level / table row-cell / list item) 출력 (JVM 11+ 필요)
opendataloader-pdf==2.3.0

# Hybrid Retrieval (Path A — Dense + BM25)
# rank_bm25: BM25Okapi (in-memory, pure Python). 한국어 짧은 단어 query 의 lexical match 보강.
# kiwipiepy: Kiwi 형태소 분석기 (한국어 STT 변형/조사 분리).
rank_bm25==0.2.2
kiwipiepy==0.23.1

# post-call Agent
typing_extensions

# OAuth token encryption (SaaS tenant integration)
cryptography==41.0.7
